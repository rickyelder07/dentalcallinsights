# Copy this file to .env.local and fill in your actual values
# DO NOT commit .env.local to git
#
# Current Status: ✅ Milestone 2 Complete, 🚧 Milestone 3 In Progress
# Branch: milestone/03-audio-upload-and-storage

# ==================================
# SUPABASE CONFIGURATION (Required)
# ==================================
# Get these from your Supabase project settings (https://app.supabase.com)
# Navigate to: Project Settings → API

# Public URL - Safe to expose to client
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Anon/Public Key - Safe to expose to client (RLS-protected)
# Used for client-side auth and database queries
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Service Role Key - NEVER expose to client! Server-side only!
# Bypasses Row Level Security - use with extreme caution
# Only use in API routes, server components, or backend scripts
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ==================================
# AI/ML APIS (Required for features)
# ==================================

# OpenAI API Key for transcription and embeddings
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# ==================================
# INNGEST CONFIGURATION (Required for long transcriptions)
# ==================================

# Inngest Event Key for background job processing
# Get from: https://app.inngest.com → Settings → Keys
# Required for handling long-running transcriptions (>5 minutes)
INNGEST_EVENT_KEY=your-inngest-event-key-here

# ==================================
# DEPLOYMENT (Optional)
# ==================================

# Vercel deployment URL (auto-populated on Vercel)
# For local development, use http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ==================================
# DATABASE (Optional - Direct Connection)
# ==================================

# Direct PostgreSQL connection (only needed for advanced use cases)
# Get from: Supabase Project Settings → Database → Connection String
# DATABASE_URL=postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres

# ==================================
# NOTES
# ==================================
# - NEXT_PUBLIC_* variables are exposed to the browser
# - Keep SUPABASE_SERVICE_ROLE_KEY secure (server-side only)
# - Restart dev server after changing environment variables
# - Never commit .env.local to version control
# - See README.md for detailed setup instructions
